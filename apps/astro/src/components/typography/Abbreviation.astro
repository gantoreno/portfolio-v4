---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"abbr">;

const { class: classes, ...rest } = Astro.props;
---

<abbr
  class:list={[
    "dotted decoration-disabled decoration-1 underline-offset-2",
    classes,
  ]}
  {...rest}><slot /></abbr
>
