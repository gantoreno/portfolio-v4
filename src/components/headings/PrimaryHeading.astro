---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"h1">;

const { class: classNames, ...rest } = Astro.props;
---

<h1
  class:list={["text-primary font-bold text-2xl mb-[20px]", classNames]}
  {...rest}
>
  <slot />
</h1>
