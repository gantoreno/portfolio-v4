---
type Props = {
  name: string;
};

const { name } = Astro.props;
---

<img
  class="inline-block self-center rounded-sm ml-[0.5ch]"
  src={`/assets/img/memes/${name}.webp`}
  width="20"
  height="20"
/>
