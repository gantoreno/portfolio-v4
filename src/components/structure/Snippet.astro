---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"pre">;

const { class: classes, ...rest } = Astro.props;
---

<pre
  data-blurrable
  class:list={[
    "p-[20px] mb-[20px] rounded-md [&>code]:p-0 [&>code:before]:content-[''] overflow-x-scroll [&>code:after]:content-[''] shiki",
    classes,
  ]}
  {...rest}><slot /></pre>
