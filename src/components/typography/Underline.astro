---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"u">;

const { class: classes, ...rest } = Astro.props;
---

<u
  class:list={[
    "decoration-disabled underline decoration-1 underline-offset-2",
    classes,
  ]}
  {...rest}><slot /></u
>
